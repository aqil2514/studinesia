import { Great_Vibes, Rubik } from "next/font/google";

export const greatVibes = Great_Vibes({
  weight: "400",
  subsets: ["latin"],
});

export const rubik = Rubik({
  weight: ["400", "600"],
  subsets: ["latin"],
});
